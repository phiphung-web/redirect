<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>System Status</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light p-4">
    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h3 class="mb-0">System Status</h3>
            <a href="/" class="btn btn-outline-secondary btn-sm">Quay lại Dashboard</a>
        </div>
        <div class="row g-3 mb-4">
            <div class="col-md-3"><div class="card shadow-sm"><div class="card-body"><div class="text-muted">Uptime (OS)</div><h5 class="fw-bold"><%= Math.round(uptime/3600) %> h</h5></div></div></div>
            <div class="col-md-3"><div class="card shadow-sm"><div class="card-body"><div class="text-muted">Uptime (App)</div><h5 class="fw-bold"><%= Math.round(procUptime/3600) %> h</h5></div></div></div>
            <div class="col-md-3"><div class="card shadow-sm"><div class="card-body"><div class="text-muted">Load 1m</div><h5 class="fw-bold"><%= load1.toFixed(2) %></h5></div></div></div>
            <div class="col-md-3"><div class="card shadow-sm"><div class="card-body"><div class="text-muted">DB</div><h5 class="fw-bold <%= dbStatus==='ok' ? 'text-success' : 'text-danger' %>"><%= dbStatus %></h5></div></div></div>
        </div>
        <div class="row g-3">
            <div class="col-md-6">
                <div class="card shadow-sm h-100">
                    <div class="card-header bg-white fw-bold">Memory</div>
                    <div class="card-body">
                        <p>Tổng: <%= Math.round(memTotal/1024/1024) %> MB</p>
                        <p>Đang dùng: <%= Math.round(memUsed/1024/1024) %> MB</p>
                        <p>Còn trống: <%= Math.round(memFree/1024/1024) %> MB</p>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card shadow-sm h-100">
                    <div class="card-header bg-white fw-bold">Process</div>
                    <div class="card-body">
                        <p>Platform: <%= platform %> <%= release %></p>
                        <p>CPUs: <%= cpus %></p>
                        <p>Process RSS: <%= Math.round(procMem.rss/1024/1024) %> MB</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
